Q: SLB的请求延时监控“HTTP请求到后端的延时”
这个是指监听器到后端服务器的延时，还是从客户端到RS的延时？
另外这个延时超过多久，监听器就把连接丢弃，直接返回5xx？
是按照监听器的超时时间60秒么？https://docs.ksyun.com/documents/1150  监听器超时时间官网说明。

A：
1. 延时是LB到RS这段的，延时的时长相当于：LB-->RS LB<---RS 两段时间之和。
2. 超时时间是60秒。如果超时断开，则返回5XX，监听器状态码侧能看到对应的监控数据。

